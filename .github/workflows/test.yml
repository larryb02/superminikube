name: Test
on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: docker:dind
      options: --privileged
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Start dependencies
        run: |
          docker network create ci-network; \
          docker run -d --name ci-redis --network ci-network redis

      - name: Install redis-cli
        run: apk --update add redis

      - name: Is it alive?
        run: |
          for i in $(seq 1 20); do
            if docker exec ci-redis redis-cli ping | grep -q PONG; then
              echo "Redis is ready"
              break
            fi
            sleep 1
          done

      # - name: Run tests
      #   run: docker-compose -f ci/compose.yml up -d && go test -v ./...
